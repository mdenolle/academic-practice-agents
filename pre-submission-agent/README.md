# Denolle Pre-Submission Reviewer

A pre-submission peer reviewer for geoscience manuscripts, published openly as part
of [`academic-practice-agents`](https://github.com/mdenolle/academic-practice-agents).
One orchestrator agent calls nine focused subagents — seven for the paper's
sections (abstract, introduction, methods, results, discussion, conclusions,
figures/data), one for reproducibility, and one for citation & idea diversity —
then folds their findings into the eight-criterion Denolle rubric and writes a
submission-readiness report. On a re-review it runs incrementally: it reconciles
each prior finding and raises new issues only on text the revision changed (see
*Iterative review, provenance & versioning* below).

It also has an optional **Author Verification** pass — an interactive,
oral-exam-style quiz that interrogates the authors on the data-processing,
signal-processing, and interpretation choices they must own, records their
answers to a verification log, and emits a process-only in-paper statement. It is
a countermeasure to over-reliance: as the review side gets better at critiquing
the paper, this side keeps the human in control of it (see *Author Verification*
below).

The review is **advisory**. A human approves every finding before submission, and
it is intended for your own unpublished work — never another group's manuscript,
which would breach journal confidentiality.

---

## What's in here

```
denolle-pre-submission-reviewer/
├── README.md                         ← you are here (install guide)
├── AGENTS.md                         ← cross-tool entry point (Codex, Cursor, Copilot, Gemini, Windsurf)
├── skills/
│   └── pre-submission-reviewer/      ← THE SOURCE OF TRUTH — edit only here
│       ├── SKILL.md                  ← the orchestrator
│       ├── references/               ← nine subagents + S-AV author quiz + manifest schema
│       │   ├── section_abstract.md           (S-AB)
│       │   ├── section_introduction.md       (S-IN)
│       │   ├── section_methods.md            (S-ME)
│       │   ├── section_results.md            (S-RE)
│       │   ├── section_discussion.md         (S-DI)
│       │   ├── section_conclusions.md        (S-CO)
│       │   ├── section_figures_data.md       (S-FD)
│       │   ├── section_reproducibility.md    (S-RP)
│       │   ├── section_citation_diversity.md (S-CD)
│       │   ├── section_author_verification.md (S-AV — interactive author quiz)
│       │   ├── author_profile.md             (persona layer)
│       │   └── review_manifest.md            ← provenance + iteration ledger + verification-log schema
│       └── profiles/                 ← per-author voice profiles (default.md, TEMPLATE.md)
├── scripts/
│   └── detect_changes.py             ← diff wrapper: latexdiff / before-after pair → changes.json
├── reviews/                          ← per-manuscript review manifests (state lives here)
├── .claude/
│   ├── skills/                       ← Claude Code project copy (made by install.sh)
│   └── agents/                       ← nine subagent personas, generated by install.sh (optional, advanced)
├── .cursor/rules/
│   └── pre-submission-reviewer.mdc   ← Cursor rule
├── .github/
│   ├── copilot-instructions.md       ← repo-wide Copilot context
│   └── prompts/
│       └── pre-submission-review.prompt.md   ← /pre-submission-review in VS Code
└── scripts/
    └── install.sh                    ← wires Claude Code/Desktop + builds the claude.ai zip
```

**One source of truth.** The real agent is `skills/pre-submission-reviewer/`.
Every file under `.claude/`, `.cursor/`, and `.github/` is a thin pointer to it,
so there is one place to edit and no copies to keep in sync. (The exception is
`.claude/skills/`, a working copy that `install.sh` regenerates — don't hand-edit
it.)

---

## Getting it

This reviewer lives in the public `academic-practice-agents` repo under
`pre-submission-agent/`. Clone the repo and you have it:

```bash
git clone https://github.com/mdenolle/academic-practice-agents.git
cd academic-practice-agents/pre-submission-agent
```

The Cursor, Copilot, and Codex setups work on clone with no extra step, because
those tools read the adapter files already committed here. Claude needs one
install step (`bash scripts/install.sh`). Then follow the section for your tool
below.

---

## Install per tool

**Which file each tool reads, and where it must live.** You author once in
`skills/pre-submission-reviewer/`; each tool picks the agent up through its own
adapter. Only Claude needs a copy step (run `scripts/install.sh`); the rest work
on clone because their adapter files are committed at the paths they look in.

| Tool | Reads | Where it must live | Install step |
|---|---|---|---|
| **Claude Code** | the skill + generated personas | `~/.claude/skills/` (global) and/or repo `.claude/skills/` + `.claude/agents/` | `bash scripts/install.sh` (regenerates both) |
| **Claude.ai** | the skill, zipped | uploaded in Settings → Skills | `bash scripts/install.sh` → upload `dist/…zip` |
| **Cursor** | `.cursor/rules/pre-submission-reviewer.mdc` | repo root `.cursor/rules/` | none — committed |
| **Copilot (VS Code)** | `.github/copilot-instructions.md` + `.github/prompts/pre-submission-review.prompt.md` | repo root `.github/` | none — enable instruction files |
| **Codex** | `AGENTS.md` | repo root (or `~/.codex/AGENTS.md` for a global default) | none — committed |

All adapters are **thin pointers** back to `skills/pre-submission-reviewer/`, so
there is one place to edit. The `.claude/` copies are *generated* — re-run
`scripts/install.sh` after editing the source, and don't hand-edit them.

### Claude.ai (web or desktop app)

1. Build the upload bundle: `bash scripts/install.sh` (creates
   `dist/pre-submission-reviewer.zip`). If you'd rather not run the script, just
   zip the `skills/pre-submission-reviewer` folder yourself.
2. In claude.ai: **Settings → Capabilities → Skills → Upload skill**, choose the
   zip.
3. Ask Claude to "review my manuscript before submission" and attach the draft.
   Claude loads the skill automatically.

### Claude Code

1. From the repo root run `bash scripts/install.sh`. It copies the skill to
   `~/.claude/skills/` (available in every project) **and** to this repo's
   `.claude/skills/` (so it travels with the repo).
2. Restart Claude Code, or start a session inside the repo.
3. Say "run a pre-submission review on this paper." The orchestrator skill fires
   on its own. Optionally it dispatches the nine personas in `.claude/agents/`
   (generated by the script) as parallel subagents.

Manual alternative (no script): copy `skills/pre-submission-reviewer` into
`~/.claude/skills/` (personal) or the project's `.claude/skills/` (per-repo).

### Cursor

Nothing to install — `.cursor/rules/pre-submission-reviewer.mdc` is already in
the repo. Open the repo in Cursor, then in chat either ask to review a
manuscript or call it directly with `@pre-submission-reviewer`. The rule loads
the skill and its subagents. (Confirm rules are on under **Cursor Settings →
Rules**.)

### VS Code + GitHub Copilot

1. Open the repo in VS Code with Copilot Chat enabled.
2. Turn on prompt/instruction files: Settings → search **"instruction files"** →
   enable *Use Instruction Files*. (Prompt files are read from `.github/prompts/`.)
3. In Copilot Chat, run `/pre-submission-review` and attach or paste the
   manuscript. Repo context comes from `.github/copilot-instructions.md`.

### Codex (CLI or app)

Nothing to install — Codex reads `AGENTS.md` at the repo root automatically.

1. `cd` into the repo and start Codex.
2. Ask it to review the manuscript before submission. It follows the agent
   section of `AGENTS.md`, which points to the skill and subagents.

For a personal default across all repos, copy the agent section of `AGENTS.md`
into `~/.codex/AGENTS.md`.

---

## Iterative review, provenance & versioning

The reviewer is **stateful across drafts**. The first review is exhaustive and
builds an **Issue Ledger** (every finding gets a durable ID). Each later review is
**incremental**: it reconciles each prior finding (`RESOLVED` / `PARTIALLY
ADDRESSED` / `NOT ADDRESSED` / `REGRESSED`), raises new issues *only* on text the
revision changed, and never re-litigates unchanged sections — so every iteration
is a strict improvement on the last.

To see what changed between drafts, give it one of: a **latexdiff** file, a
**before/after `.tex`** pair, or a **before/after `.md`** pair. The wrapper
normalizes any of them:

```bash
# before/after pair (LaTeX or Markdown) — runs latexdiff if installed
python3 scripts/detect_changes.py --old draft_v1.tex --new draft_v2.tex \
    --manuscript-id denolle2026-tremor --out reviews/denolle2026-tremor.changes.json

# or a latexdiff file you already have
python3 scripts/detect_changes.py --diff diff.tex --manuscript-id denolle2026-tremor
```

That `changes.json` tells the orchestrator which subagents to re-run. State and
**provenance** (skill version, model, iteration count, manuscript hash, full
ledger history) live in `reviews/<manuscript-id>.review.json` — schema in
[`references/review_manifest.md`](skills/pre-submission-reviewer/references/review_manifest.md).
Every report ends with an **AI-review disclosure stamp** to paste into the
manuscript's AI-use statement (records *process*, never endorsement) and a
copy-pasteable **Ledger for next iteration**.

> **Not for the browser past iteration 1.** Iterative review needs to persist the
> ledger between runs, so this tool is built for a CLI / filesystem-backed agent
> (Claude Code, Codex, Cursor). A stateless claude.ai Skills session has nowhere
> to store the manifest, so the no-new-issues and monotonicity guarantees cannot
> be enforced there. A first, single-shot review works in the browser; for
> iteration 2+ use a CLI tool (or, as a degraded fallback, manually paste the
> *Ledger for next iteration* block back in each time).

## Author Verification (keeping the human in control)

The better a review tool gets, the greater the temptation to let it own the
paper. The **Author Verification** pass exists to resist that. It does not review
the manuscript — it **examines the authors**, oral-exam style, on the choices a
scientist must own: the **data-processing and signal-processing workflow first**
(filters, windows, thresholds, normalization, stacking, detection, inversion,
ML-split independence), then method, claims, interpretation, limitations,
novelty, and figures.

Ask for it directly — "quiz me on my paper," "verify I understand my methods,"
"check I'm in control of the workflow" — or run it after a review, where it
targets the exact places the review found the paper stating *what* but not *why*
(every `REPRODUCTION-STOP`, every unjustified method choice). It asks one question
at a time, questions that **can't be answered by re-reading the paper**, records
your answers verbatim, and writes a transcript to
`reviews/<manuscript-id>.verification.json`.

What it is, and is not:

- **It certifies process, not competence.** It cannot measure understanding — a
  determined author could route the questions through another model — so it never
  claims to. The value is the transcript: your own account of your choices, on the
  record, for a co-author or the PI to read and judge.
- **The agent does not grade.** It asks, probes once if an answer is vague, and
  records whether each question was *answered* — never whether you are *right*. A
  named human adjudicates.
- **Live over typed.** A human-conducted oral exam is the high-integrity mode;
  typed answers entered in the same session are marked *degraded*.
- **It gates nothing.** A completed pass does not clear a paper and never overrides
  a soundness / reproducibility / evidence finding from the review.
- **It emits a process-only in-paper statement** you may include (or not) in the
  AI-use / Acknowledgments section — worded to record that the examination
  happened, never that understanding was proven.

This is a starting point, not a tamper-proof credential. Its credibility rests on
the human who reads the transcript — most naturally the advisor, as a
student↔advisor conversation about the methods — not on a badge.

## How the group uses it

Whatever the tool, the trigger is the same: ask for a pre-submission review of a
specific draft, name the target journal and paper type, and attach the
manuscript. You get back a section-by-section to-do list, the eight criteria
scored, strengths first, and one recommendation: ready / revise before
submission / major revision / not ready. Optionally follow it with an Author
Verification pass (above).

Bring back a `REPRODUCTION-STOP` you disagree with — that is how we tune the
subagents.

---

## Updating

Edit `skills/pre-submission-reviewer/SKILL.md` or a file in `references/`, commit,
push. Claude Code and Claude.ai users re-run `scripts/install.sh` (or re-upload
the zip). Cursor, Copilot, and Codex pick up the change on next pull, since their
adapters point at the same files.

---

## License

Released under the MIT License of the `academic-practice-agents` repo (see
[`LICENSE`](../LICENSE)). It packages the Denolle group rubric and subagent text
grounded in best-practice literature; reuse and adapt it for your own group's
pre-submission workflow. The rubric and journal calibrations reflect geoscience
and seismology norms — adjust them for other fields.
